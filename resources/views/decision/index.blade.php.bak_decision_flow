<x-layouts.base title="Feeds Putusan">
    <div>
        <div class="page-head">
            <div>
                <div class="eyebrow">Direktori Putusan Mahkamah Agung</div>
                <h1 class="page-title">Feeds Putusan</h1>
                <p class="page-desc">Putusan terbaru Mahkamah Agung. Ditampilkan langsung dari sumber, tidak disimpan ke database.</p>
            </div>
            <form method="GET" action="{{ route('decisions') }}" style="margin:0">
                <button type="submit" class="btn btn-primary">↻ Refresh Feed</button>
            </form>
        </div>

        <div class="card" style="padding:12px 16px;margin-bottom:16px;display:flex;gap:8px;align-items:center;flex-wrap:wrap;font-size:12px">
            <span style="color:var(--muted)">Sumber:</span>
            <a href="{{ $rawRssUrl }}" target="_blank" rel="noopener" style="color:var(--accent);word-break:break-all">Direktori Putusan MA</a>
            <span style="margin-left:auto;color:var(--muted)">{{ count($decisions) }} putusan terbaru</span>
        </div>

        @if($errorMsg)
            <div class="card" style="padding:24px;text-align:center">
                <div style="color:var(--err);font-weight:600;font-size:13px">Feed belum dapat dimuat</div>
                <div style="color:var(--muted);font-size:12px;margin-top:6px">{{ $errorMsg }}</div>
                <a href="{{ $rawRssUrl }}" target="_blank" rel="noopener" class="btn btn-secondary" style="margin-top:16px">Buka sumber ↗</a>
            </div>
        @elseif(count($decisions))
            <div class="card" style="padding:0;overflow:hidden">
                @foreach($decisions as $decision)
                    <article style="padding:18px 20px;border-bottom:1px solid var(--line)">
                        <div style="display:flex;gap:14px;align-items:flex-start">
                            <span style="color:var(--accent);font-size:17px;line-height:1">⚖</span>
                            <div style="min-width:0;flex:1">
                                <a href="{{ $decision['link'] }}" target="_blank" rel="noopener" style="font-size:14px;font-weight:650;line-height:1.45;color:var(--text);display:block">{{ $decision['title'] }}</a>
                                @if($decision['tags'])
                                    <div style="margin-top:8px;font-size:12px;color:var(--accent)">{{ $decision['tags'] }}</div>
                                @endif
                                <div style="margin-top:8px;display:flex;gap:12px;flex-wrap:wrap;font-size:11px;color:var(--muted)">
                                    @if($decision['dates'])<span>{{ $decision['dates'] }}</span>@endif
                                    @if($decision['pubDate'])<span>Feed: {{ $decision['pubDate'] }}</span>@endif
                                    <a href="{{ $decision['link'] }}" target="_blank" rel="noopener" style="color:var(--accent)">Buka putusan ↗</a>
                                </div>
                            </div>
                        </div>
                    </article>
                @endforeach
            </div>
        @else
            <div class="card" style="padding:24px;text-align:center;color:var(--muted);font-size:13px">Tidak ada putusan pada feed saat ini.</div>
        @endif
    </div>
</x-layouts.base>
